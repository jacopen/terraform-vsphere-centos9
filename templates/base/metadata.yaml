local-hostname: ${hostname}
instance-id: ${hostname}
network:
  version: 2
  ethernets:
    ens33:
      dhcp4: false
      addresses:
        - ${ip_address}/${subnet_mask}
      gateway4: ${gateway}
      nameservers:
        addresses: [${nameservers}]

